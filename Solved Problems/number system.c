#include<stdio.h>
#define N=10 

int main( )
{
    int arry[N],i=0,j,k,n,rem;
   printf("input the number of n "); printf( "\n");
   scanf("%d",&n);
   while(n>o)
   {
    rem = n/2;
    arry[i] = rem;
    i++;

   }
   prinf(" printing the number in binary format "); printf( "\n");
   printf("-----------------------------------------------------");
   for(j=0;j<n;j++)
   {
       printf(" %d",arry[j]);
   }
}